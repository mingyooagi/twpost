[project]
name = "twpost"
version = "0.1.0"
description = "Post tweets via Clawdbot's browser CDP connection"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40.0",
]

[project.scripts]
twpost = "twpost:main"

[dependency-groups]
dev = []
